# KWISMO Modèle IA — dépendances Python

# --- Manipulation de données ---
pandas>=2.2
numpy>=1.26

# --- Modèle A : scoring de réputation (arbres boostés, CPU) ---
scikit-learn>=1.5
lightgbm>=4.3
joblib>=1.4

# --- Modèle B : NLP multilingue (AfroXLMR + PEFT/LoRA) ---
torch>=2.3
transformers>=4.42
peft>=0.11
sentencepiece>=0.2.0

# --- Service d'inférence (FastAPI) ---
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30
pydantic>=2.7,<3.0
pydantic-settings>=2.3
slowapi>=0.1.9        # limitation de débit (protection anti-surcharge)

# --- Appels HTTP (collecte via le backend, tests) ---
httpx>=0.27

# --- Scraping & OCR (collecte de données locales pour le Modèle B) ---
beautifulsoup4>=4.12   # parsing HTML des pages découvertes
playwright>=1.45       # navigateur automatisé (Facebook/Instagram/X) — lancer `playwright install chromium` après l'install
easyocr>=1.7           # extraction de texte depuis les captures d'écran
imagehash>=4.3          # dédoublonnage d'images quasi identiques
Pillow>=10.0
duckduckgo_search>=6.0 # découverte dynamique de sites (recherche web, sans clé API)
matplotlib>=3.8         # graphes d'évolution de la collecte (src/data/metrics.py)
seaborn>=0.13.0        # visualisations graphiques avancées des notebooks

# --- Notebooks (exploration & entraînement local/Colab/Kaggle) ---
jupyter>=1.0

# --- Tests ---
pytest>=8.2

# --- Qualité de code ---
ruff>=0.5
black>=24.4
