# KWISMO Backend — dependances Python

# --- Framework API & serveur ASGI ---
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30
gunicorn>=22.0

# --- ORM (backend hybride SQLite/PostgreSQL/MySQL) ---
prisma>=0.13

# --- Validation & configuration ---
pydantic>=2.7,<3.0
pydantic-settings>=2.3
email-validator>=2.1
python-dotenv>=1.0

# --- Authentification & securite ---
python-jose[cryptography]>=3.3
argon2-cffi>=23.1

# --- Cache / limitation de debit / files d'attente ---
redis>=5.0
rq>=1.16
slowapi>=0.1.9        # rate limiting (anti brute-force / anti DoS)

# --- Appels HTTP vers le service IA ---
httpx>=0.27

# --- Tests ---
pytest>=8.2
pytest-asyncio>=0.23

# --- Qualite de code ---
ruff>=0.5
black>=24.4
mypy>=1.10
